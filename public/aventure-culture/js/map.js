let mapWidth = innerHeight / 1080 * 9600;

let map = {
    x: 0,
    y: 0,
    xSpeed: 0,
    width: mapWidth,
    height: innerHeight,
    frontPlane: document.getElementById("front-plane"),
    backPlane: document.getElementById("back-plane")
}